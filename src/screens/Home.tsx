import React from "react";
import { Text, View } from "react-native";
import FormModal from "../components/FormModal";

const Home = () => {
  return (
    <View>
      <FormModal />
    </View>
  );
};

export default Home;
